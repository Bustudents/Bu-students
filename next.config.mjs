/** @type {import('next').NextConfig} */
const nextConfig = {
      basePath: "//Bu-students",
    output: "export",  // <=== enables static exports
    reactStrictMode: true,};

export default nextConfig;

